# Local build artifacts, caches, virtual environments, and SQLite files.
# These are intentionally untracked because they are machine-specific or
# regenerated during normal development and CI runs.
__pycache__/
*.py[cod]
*.sqlite3
*.db
.pytest_cache/
.ruff_cache/
.venv/
dist/
build/
.mypy_cache/
coverage.xml

.env